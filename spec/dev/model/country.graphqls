type Country @rootEntity
        @roles(readWrite: ["allusers"])
        @namespace(name: "foundation") {
    isoCode: String @key @unique
    description: [Translation]
    totalInvestment: String @roles(readWrite: "accounting") @index
}
