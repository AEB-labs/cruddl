RETURN {
  "not_like": (
    FOR v_paper1
    IN @@papers
    FILTER !((v_paper1.`key` >= UPPER(@var1) && v_paper1.`key` < LOWER(@var2)))
    SORT (v_paper1.`key`)
    RETURN {
      "key": v_paper1.`key`
    }
  )
}
