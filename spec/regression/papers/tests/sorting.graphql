query {
    allPapers(orderBy: title_ASC) {
            title
            readers(orderBy: firstName_DESC) {
                    firstName
                }
    }
    other: allPapers(orderBy: isPublished_DESC) {
                isPublished
    }
}