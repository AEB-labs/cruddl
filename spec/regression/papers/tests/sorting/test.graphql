query string {
    allPapers(orderBy: title_ASC) {
        title
        readers(orderBy: firstName_DESC) {
            firstName
        }
    }
}

query boolean {
    allPapers(orderBy: isPublished_DESC) {
        isPublished
    }
}
