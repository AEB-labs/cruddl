query unordered {
    Delivery(deliveryNumber: "D1") {
        allItems(skip: 1, first: 1) {
            itemNumber
        }
    }
}

query ordered {
    Delivery(deliveryNumber: "D1") {
        allItems(orderBy: itemNumber_DESC, skip: 1, first: 1) {
            itemNumber
        }
    }
}
