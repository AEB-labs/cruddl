mutation update {
    updateDelivery(input: { id: "@{ids/Delivery/1}", shippedAt: "2018-06-06T10:37:00Z" }) {
        shippedAt
    }
}

query query {
    Delivery(id: "@{ids/Delivery/1}") {
        shippedAt
    }
}
