type NumberRange @rootEntity @roles(readWrite: ["allusers"]) {
    name: NumberRangeName @key
    number: Int @calcMutations(operators: [ADD, MULTIPLY])
}

enum NumberRangeName {
    DELIVERY
    HANDLING_UNIT
}
