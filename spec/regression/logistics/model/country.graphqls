type Country @rootEntity @roles(readWrite: ["allusers"]), @namespace(name: "foundation"){
    isoCode: String @key
    description: [Translation]
    totalInvestment: String @roles(readWrite: "accounting")
}
